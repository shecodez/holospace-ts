/*
import { User } from "@/store/Users/state";

export interface Me extends User {
  email: string;
}
//TODO: add getter holoTag return `${user.name}#{user.pin}`

export type State = {
  isLoading: boolean;
  me: Me | undefined;
};

export const state: State = {
  isLoading: false,
  me: undefined,
};

*/
