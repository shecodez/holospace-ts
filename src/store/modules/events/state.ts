/*
import { User } from "@/store/Users/state";

export interface Invitee extends User {
  isAttending: boolean;
}

export type Event = {
  id: number;
  title: string;
  details?: string;
  startDate: Date;
  endDate: Date;
  isReocurring: boolean;
  owner: number; // User, Deck, DiskSpace
  inviteeIds?: Invitee[];
  //lastModBy: number; // UserId
  //createdAt: serverTimestamp();
  //updatedAt: null;
};

export type State = {
  isLoading: boolean;
  list: Event[];
};

export const state: State = {
  isLoading: false,
  list: [],
};
*/
